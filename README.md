
  # Animate Scene Sequence

  This is a code bundle for Animate Scene Sequence. The original project is available at https://www.figma.com/design/unkK8xf20Qi7E97eNwAWHV/Animate-Scene-Sequence.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  